<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/mybatis-3/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.foodfactory.dx.mapper.ItemMapper">

    <resultMap id="itemResultMap" type="com.foodfactory.dx.domain.Item">
        <id column="item_id" property="itemId"/>
        <result column="name" property="name"/>
        <result column="safety_stock_qty" property="safetyStockQty"/>
        <result column="target_stock_qty" property="targetStockQty"/>
        <result column="standard_batch_qty" property="standardBatchQty"/>
        <result column="shelf_life_days" property="shelfLifeDays"/>
        <result column="hydration_ratio_min" property="hydrationRatioMin"/>
        <result column="hydration_ratio_max" property="hydrationRatioMax"/>
        <result column="hydration_qty_min" property="hydrationQtyMin"/>
        <result column="hydration_qty_max" property="hydrationQtyMax"/>
        <result column="is_active" property="active"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <insert id="insert" parameterType="com.foodfactory.dx.domain.Item"
            useGeneratedKeys="true" keyProperty="itemId">
        INSERT INTO items (
            name, safety_stock_qty, target_stock_qty, standard_batch_qty, shelf_life_days,
            hydration_ratio_min, hydration_ratio_max, hydration_qty_min, hydration_qty_max, is_active
        )
        VALUES (
            #{name}, #{safetyStockQty}, #{targetStockQty}, #{standardBatchQty}, #{shelfLifeDays},
            #{hydrationRatioMin}, #{hydrationRatioMax}, #{hydrationQtyMin}, #{hydrationQtyMax}, #{active}
        )
    </insert>

    <select id="findById" resultMap="itemResultMap">
        SELECT * FROM items WHERE item_id = #{itemId}
    </select>

    <select id="findAll" resultMap="itemResultMap">
        SELECT * FROM items ORDER BY item_id
    </select>

    <select id="findByFilters" resultMap="itemResultMap">
        SELECT * FROM items
        <where>
            <if test="active != null">
                AND is_active = #{active}
            </if>
        </where>
        ORDER BY item_id
    </select>

    <update id="update" parameterType="com.foodfactory.dx.domain.Item">
        UPDATE items
        SET name                = #{name},
            safety_stock_qty    = #{safetyStockQty},
            target_stock_qty    = #{targetStockQty},
            standard_batch_qty  = #{standardBatchQty},
            shelf_life_days     = #{shelfLifeDays},
            hydration_ratio_min = #{hydrationRatioMin},
            hydration_ratio_max = #{hydrationRatioMax},
            hydration_qty_min   = #{hydrationQtyMin},
            hydration_qty_max   = #{hydrationQtyMax}
        WHERE item_id = #{itemId}
    </update>

    <update id="setActive">
        UPDATE items
        SET is_active = #{active}
        WHERE item_id = #{itemId}
    </update>

</mapper>
