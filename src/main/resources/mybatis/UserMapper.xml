<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.foodfactory.dx.mapper.UserMapper">

    <resultMap id="userResultMap" type="com.foodfactory.dx.domain.User">
        <id column="user_id" property="userId"/>
        <result column="username" property="username"/>
        <result column="password_hash" property="passwordHash"/>
        <result column="access_level" property="accessLevel"/>
        <result column="is_active" property="isActive"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <insert id="insert" parameterType="com.foodfactory.dx.domain.User"
            useGeneratedKeys="true" keyProperty="userId">
        INSERT INTO user (username, password_hash, access_level, is_active)
        VALUES (#{username}, #{passwordHash}, #{accessLevel}, #{isActive})
    </insert>

    <select id="findById" resultMap="userResultMap">
        SELECT * FROM user WHERE user_id = #{userId}
    </select>

    <select id="findByUsername" resultMap="userResultMap">
        SELECT * FROM user WHERE username = #{username}
    </select>

    <select id="findAll" resultMap="userResultMap">
        SELECT * FROM user ORDER BY user_id ASC
    </select>

    <update id="updatePasswordHash">
        UPDATE user SET password_hash = #{passwordHash} WHERE user_id = #{userId}
    </update>

    <update id="updateActive">
        UPDATE user SET is_active = #{isActive} WHERE user_id = #{userId}
    </update>

</mapper>
